'use strict';

var r42 = require('../lib/');
var path = require('path');

r42.config({
  baseDir: path.join(__dirname, 'deps'),

  paths: {
    _: 'lodash',
  },
});

r42.inject(function (test) {
  console.log('==', test);
});
