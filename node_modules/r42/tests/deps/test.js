'use strict';

define(function (_, util, elt) {
  var inspect = _.createCallback(_.partialRight(util.inspect, { colors: true }), null, 1);
  var print = _.compose(console.log, inspect);
  _.each(['test', 42, elt().join(';')], print);

  return 21;
});
